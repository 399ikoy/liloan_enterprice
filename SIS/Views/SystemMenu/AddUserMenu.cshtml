@model SIS.Models.System_Menus

@{
    ViewBag.Title = "Add Account";
}

<h2>Edit</h2>

@using (Html.BeginForm("AddUserMenu","SystemMenu",FormMethod.Post)) {
    @Html.ValidationSummary(true)
    @Html.Hidden("system_menus_id", Model.system_menus_id)
    


        <div class="editor-label">
            User
        </div>
        <div class="editor-field">
            @Html.DropDownList("system_menus_id")
        </div>

        
        <p>
            <input type="submit" value="Save" />
        </p>
}


@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
